import React from 'react';
import Header from '../components/Header';

const CreatePage = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default CreatePage;