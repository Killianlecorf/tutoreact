import React from 'react';
import BlocInfo from '../components/BlocInfo';

const pageInfo = () => {
    return (
        <div>
            <BlocInfo />
        </div>
    );
};

export default pageInfo;